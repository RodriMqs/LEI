Classe é o modelo a partir do qual criamos objetos

por exemplo uma planta de uma casa
a planta não é a casa mas é o modelo que indica como deve ser construida

Do exemplo anterior (PESSOA)
	classe pessoa é o modelo

class Pessoa{
	string nome;
	int idade;
}

CLass é o modelo , todos os objetos criados a partir desta class(modelo) vao ter uma string nome e um int idade
